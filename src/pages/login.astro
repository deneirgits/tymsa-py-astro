---
import LoginForm from "../components/LoginForm.svelte";
import Layout from "../layouts/Layout.astro";

if (Astro.cookies.get("accessToken")) {
  return Astro.redirect("/");
}
---

<Layout title="Login">
  <LoginForm client:load />
</Layout>
